Modulo bibliotecaImpl implementa Biblioteca{
    var posicionLibro: dictLog<idLibro,nat> // nat es la posicion
    var librosSocios: dictDigital<socio, conjLog<idLibro>>
    var posicionesLibres: minHeap<nat>

    proc Prestados (in b:Biblioteca, in s: Socio): Conjunto<idLibro>{
      res = b.librosSocios.obtener(s) // O(1) porque |k| está acotado como max 50
    }

    proc AgregarLibroAlCatalogo(inout b: Biblioteca, in l: idLibro){
            a := b.posicionesLibres.buscarMinimo   // O(1)
            b.posicionesLibres.borrarMinimo        // O(log(k))
            b.posicionLibro.definir(l,a)           // O(log(L)) 
    }

    proc PedirLibro(inout b: Biblioteca, in l: idLibro, in s: Socio){
        // hacer
    }


    proc DevolverLibro(inout b: Biblioteca, in l: idLibro, in s: Socio){
         a:= b.posicionesLibres.buscarMinimo()  // O(1)
         conj:= b.librosSocios.obtener(s)       // O(1)
         conj.sacar(l)                          // O(log(r))
         b.posicionesLibres.borrarMinimo   // O(log(k))
         b.posicionLibro.definir(l,a)      // O(log(L)) 

    }








}




// Sorting


Se puede crear estructuras Por ejemplo en el ej. del 2 parcial, se podia usar 
un dictDigital que tenga adentro Dict de trie donde se guarda el stock, 
se junta todo en un arreglo de triplas y se ordena con dos merge sort



Saber como funciona en detalle (complejidad + estable + entender funcionamiento):

Heap
Bucket 
Counting



Con saber la complejidad y si es estable alcanza:

Bubble
Merge
Insertion
Selection
(El resto)



Radix (Casi no lo tomaron)
QuickSort (Directamente ni se vió. Puede que aparezca en algun final)

(Busqueda binaria no hace falta. Es de algo 3)



Si hacer guia de estructuras. vale la pena
La guia de sorting no cambió nada. Es la misma que antigua Algo 2. En cubaWiki probablemente esté resuelta.



// Sorting


Se puede crear estructuras (Por ejemplo un dictDigital que tenga adentro)


Dict de trie donde se guarda el stock, se junta todo en un arreglo de triplas y se ordena con dos merge sort



Saber las complejidades y si es estable(Todos)




Saber como funciona en detalle:

Heap
Bucket 
Counting



Con saber la complejidad y si es estable alcanza:

Merge
Insertion
Bubble
Selection
(El resto)



Radix (Casi no lo tomaron)
QuickSort (Directamente ni se vió. Puede que aparezca en algun final)
